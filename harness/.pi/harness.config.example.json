{
  "_README": "Config OPT-IN del context-budget (msg 520). Copia in 'harness.config.json' (stessa cartella) e taralo per il TUO modello/infra. Senza file → si usano questi default (tarati per il nostro SLM 4B). Override rapidi anche via env HARNESS_* (es. HARNESS_TOKEN_MATRIOSKA_PCT=0.9).",
  "_come_tararlo": "Modello a CONTESTO PICCOLO (SLM 4B): soglie BASSE → compatta/focalizza PRIMA, resti nel regime di qualità alta. Modello a CONTESTO GRANDE (es. Claude Sonnet): soglie ALTE → compatta DOPO, sfrutti la finestra senza penalizzare le prestazioni.",
  "trigger": {
    "_tokenReorderPct": "frazione 0..1 del context window oltre cui suggerisce REORDER (riordino in-place). Default 0.55.",
    "tokenReorderPct": 0.55,
    "_tokenMatrioskaPct": "frazione 0..1 oltre cui suggerisce MATRIOSKA (zoom-in/focus). = il tuo 'contesto pieno effettivo'. Default 0.75; per Sonnet potresti alzarlo a 0.9.",
    "tokenMatrioskaPct": 0.75,
    "_watchReorder": "n. di item sott'osservazione (task aperti + verifiche pendenti) oltre cui suggerisce reorder. Default 12.",
    "watchReorder": 12,
    "_watchMatrioska": "n. di item oltre cui suggerisce matrioska. Default 25.",
    "watchMatrioska": 25,
    "_maxDepth": "profondità massima dello stack di focus annidati. Default 3.",
    "maxDepth": 3,
    "focusK": 5,
    "_outputReservePct": "frazione 0..0.9 di finestra RISERVATA a output+thinking (che condividono la window dell'input). I trigger si calcolano su window*(1-reserve) → scattano PRIMA. È una riserva FISICA, distinta da tokenMatrioskaPct (soglia comportamentale). Default 0 (off). Per un modello con thinking lungo: prova 0.15.",
    "outputReservePct": 0
  },
  "_messagesWindowN": "quanti turni recenti mostrare VERBATIM nella lane <messages_with_user>. Più alto = più storia verbatim (più token). Default 8.",
  "messagesWindowN": 8,
  "_gathering": "QUANTO è forzato il 'guarda l'ordine dei task prima di entrare a fuoco' (focus-gathering v1). mode: 'delegated' (default, il modello decide quando consultare get_execution_order — lean), 'inject' (l'harness allega la vista ordinata nel focus_hint quando nudga il focus — anti-cecità, consigliato per SLM piccoli), 'require' (enter_focus è bloccato finché non chiami get_execution_order). minTasksForForce: inject/require agiscono SOLO con ≥ N task open (sotto-soglia → no-op, anti-cerimonia). Per Sonnet: 'delegated' basta.",
  "gathering": {
    "mode": "delegated",
    "minTasksForForce": 5
  }
}
