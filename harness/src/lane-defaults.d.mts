/** Type declarations for lane-defaults.mjs (SSOT default numerici delle lane di context). */
export const DEFAULT_MESSAGES_WINDOW_N: number;
export const DEFAULT_MESSAGES_CHAR_CAP: number;

declare const _default: {
  DEFAULT_MESSAGES_WINDOW_N: number;
  DEFAULT_MESSAGES_CHAR_CAP: number;
};
export default _default;
export const DEFAULT_MAX_OPEN_FILE_VIEWS: number;
