/** Type declarations for state-paths.mjs (SSOT directory di stato). */
export const STATE_DIR: string;
export const TRACE_DIR: string;
export const REPORTS_DIR: string;

declare const _default: {
  STATE_DIR: string;
  TRACE_DIR: string;
  REPORTS_DIR: string;
};
export default _default;
